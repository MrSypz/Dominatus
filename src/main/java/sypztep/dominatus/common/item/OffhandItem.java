package sypztep.dominatus.common.item;

import io.wispforest.accessories.api.AccessoryItem;

public class OffhandItem extends AccessoryItem {
    public OffhandItem(Settings properties) {
        super(properties);
    }
}
